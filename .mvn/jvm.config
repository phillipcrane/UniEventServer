# TODO: Consider removing or using explicit profile flag instead
# Setting dev profile here forces it for ALL Maven runs (including CI tests)
# This can enable dev-only beans/endpoints and alter test behavior
-Dspring.profiles.active=dev
