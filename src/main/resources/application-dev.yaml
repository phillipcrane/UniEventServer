logging:
  level:
    root: INFO
    dk.unievent.app: DEBUG
    dk.unievent.app.infrastructure.client: DEBUG

unievent:
  media:
    ssrf:
      extra-allowed-hosts: placehold.co
  security:
    cookies:
      secure: false
    jwt:
      secret: dev-secret-key-this-must-be-changed-in-production
      refresh-secret: dev-refresh-secret-key-this-must-be-changed-in-production
